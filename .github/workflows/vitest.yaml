name: Test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]
        # Siehe unterstützten Veröffentlichungszeitplan von Node.js unter https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v4
      - run: cd frontend
